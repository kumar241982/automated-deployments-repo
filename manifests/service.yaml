apiVersion: v1
kind: Service
metadata:
  name: test-workflow
  namespace: default
  labels:
    app.kubernetes.io/name: test-workflow
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: test-workflow
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080